# ML Model Project

This directory contains files related to training and maintaining the AI
model used by the AI Roadmap Generator web application.

- `train_model.py` &#x2014; simplified script that defines and serialises the
  `RoadmapAIModel` class; run this to regenerate `model.pkl`.

You can expand this folder with raw datasets, Jupyter notebooks, or
experiments as development continues.
